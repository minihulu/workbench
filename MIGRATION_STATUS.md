# 迁移进度总览（Cloudflare Pages + Functions）

> 目标：手机在**任何网络**都能用工作台，**电脑可以关机**。
> 方案 D —— 前端静态资源 + `/api/*` Functions 同域部署在 Cloudflare，数据存 Supabase。全程 0 成本。

---

## 一、现在能用了吗？

**还不能上线。** 仓库已建好（`minihulu/workbench`，私有），但**代码还没推上去**——卡在本地推送环节（见第六节）。除此之外的准备工作基本就绪。

---

## 二、已完成

### 后端接口（13 个）

| 分组 | 接口 |
|---|---|
| 账号 | 注册、登录、登出、续期 |
| 管理 | 统计、用户列表、重置令牌、开关注册、备份 |
| 其他 | 公开配置、健康检查、GitHub 搜索代理 |

配套的 6 个基础模块（环境变量、JSON 处理、JWT 签发校验、鉴权、Supabase 访问、数据合并）也已完成。

**质量状态：类型检查干净，78 个测试全部通过。**

### 部署配置

`CLOUDFLARE.md`（274 行）已写好完整部署说明，含构建设置、环境变量清单、上线步骤。

顺带根除了一个历史事故源头：以前仓库里同时躺着 `index.html` 和 `workbench.html` 两份副本，改了一个另一个没变，导致过"明明改了代码却不生效"的排查噩梦。现在 `index.html` 由构建脚本从 `workbench.html` 自动生成，仓库里只保留一份真相。

---

## 三、本轮修掉的 3 个安全缺陷

这几个都不是"代码风格"问题，是会真出事的。

### 1. 删了号的用户还能继续用（30 天后门）

账号删除后，用户手上那张登录凭证在有效期内**依然能通过鉴权**。原因是代码在查不到用户资料时，默认按"从未撤销过"处理，直接放行。

已改为：查不到用户资料一律拒绝。

### 2. "退出所有设备"是假的

点击后界面显示成功，实际**一个设备都没退掉**。

根因是一个很隐蔽的数据库行为差异：修改数据的请求，在"成功改了一行"和"一行都没改到"这两种情况下，**返回的响应完全相同**。代码按"请求没报错"来判断成功，于是永远是成功。

这个我没有靠推理下结论，而是**建了个临时账号对真实数据库做了隔离实验**（用完立刻删号，全程没碰任何真实数据），跑了两遍确认：

| 请求写法 | 真的改到了 | 什么都没改到 |
|---|---|---|
| 默认写法 | `204`，空响应 | `204`，空响应 ← 无法区分 |
| 加一个特定请求头 | `200` + 1 条记录 | `200` + 0 条记录 |

已全部改为后者，并按"改到 0 行"抛错。

### 3. 同一个邀请码能被多个人同时用掉

检查"这个码用过没有"和"标记为已用"之间隔着三次网络请求。两个人卡在这个窗口里同时注册，都能成功。

已改为数据库层面的原子抢占：同一个码在数据库层面只可能被一个请求抢到，其余的直接落空；后续步骤失败还会自动把码释放回去。

---

## 四、一个值得说的发现：测试"全绿"是假象

修完之后有 3 个测试变红了。查下来**不是修复错了，是测试当初照着 bug 写的** —— 测试里把数据库的响应写死成了那个有问题的形态。实现和测试同时错，就永远测不出问题。

已按实测到的真实行为修正，并补上了此前**完全没人覆盖的失败分支**：

- 改到 0 行时必须报错，不能假装成功
- 用户资料缺失时必须拒绝，且不能继续往下写数据
- 抢占邀请码的条件里必须带"当前未被使用"

测试从 71 个增加到 78 个。

### 顺带钉死一个隐形雷

新增了一条**跨语言金标测试**。

背景：登录凭证的签名密钥是 64 个十六进制字符。Python 把它当**文本**用（64 字节）。如果 TypeScript 那边"聪明"地把它还原成二进制（32 字节），就变成了另一把钥匙——**所有人的登录凭证瞬间全部失效**，而且报错只会说"凭证无效"，根本查不到原因。

现在有一条测试拿 Python 真实签发的凭证去验证 TypeScript 认不认，外加一条反向断言（确认这条测试真有鉴别力，不是永远绿的摆设）。

---

## 五、待办

### 需要你做的

1. **把代码推送到 GitHub**（仓库已建好，私有）。最简单：双击 `push-to-github.cmd`
   **用真实终端运行**（不要通过 WorkBuddy 内部终端，那个环境会拦截 git 写文件）；
   若未登录 GitHub，要么 `gh auth login`，要么 `set GH_TOKEN=你的PAT` 后重跑。
   详见脚本顶部说明。不想用 GitHub 也行，直接把文件拖到 Cloudflare 网页部署。

2. **确认同步方式**（不回复就按推荐的做）：
   - 推荐：手机和电脑之间每 20 秒自动同步。需要改约 15 行前端代码。
   - 保守：前端一行不动，但换设备后要手动刷新才看得到新数据。

### 我这边继续的

3. 同步接口（拉取 / 推送 / 版本探测）—— 规格已定稿，尚未编码。
4. 上线后轮换一次数据库密钥。

---

## 六、两个已知的环境问题

1. **实现方中途被平台限流掐断**（429，跑了 45 分钟）。属于可恢复故障，代码没丢，三处修复已由我直接补完并验证。

2. **本会话里 GitHub 连接器不可用 + 沙箱拦截 git 写文件**。
   - `mcp__github__*` 工具在本会话的延迟工具索引里找不到，所以我无法直接帮你推。
   - 你双击 `push-to-github.cmd` 报 `could not write config file .git/config: Permission denied`，
     是因为它跑在了 WorkBuddy 的沙箱里——那个环境会把 git 对 `.git/*` 已有文件的写入拦掉。
     解决办法：**用系统自带的真实终端**跑（Win+R → cmd → `cd /d E:\project\workbench` → `push-to-github.cmd`）。
   - 我已经把脚本改成**完全不写 `.git/config`**（推送时直接用内联 URL），并支持 `GH_TOKEN` 无头推送；
     另外改用孤儿分支发布，仓库历史里不会含 `.workbuddy/` 笔记（含 Supabase 项目地址）。
   - 代码本身已安全落盘并通过验证（类型检查干净、78 测试全绿）。
